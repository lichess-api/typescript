{
  "name": "@lichess/api",
  "version": "0.0.2",
  "type": "module",
  "author": "GameRoMan",
  "license": "MIT",
  "scripts": {
    "gen:schemas": "bun ./scripts/yaml-to-zod && bun ./scripts/export-dir && bun run format",
    "gen:client": "bun ./scripts/yaml-to-client && bun run format",
    "gen:full": "bun ./scripts/yaml-to-zod && bun ./scripts/export-dir && bun ./scripts/yaml-to-client && bun run format",
    "test": "bun test",
    "test:types": "bunx tsc --noEmit",
    "format": "bunx biome format --write",
    "build": "bunx --bun tsdown",
    "npm:publish": "bun publish --access public"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@lichess-org/types": "^2.0.106",
    "tsdown": "^0.17.0",
    "typescript": "^5.9.3"
  },
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": "./dist/index.mjs",
    "./schemas": "./dist/schemas.mjs",
    "./package.json": "./package.json"
  }
}
